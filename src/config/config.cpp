#include "config.h"

namespace config
{
    int focal = 670;
    float sceneSize = 0.5;
    extern int maxIntensity = MAX_INTENSITY;
    extern int blockSize = BLOCK_SIZE;
    extern int athConst = ATH_CONST;
}
 